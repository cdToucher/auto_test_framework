scenario: 错误密码被登录拒绝
module: auth
priority: P1
tags: [negative, api, shoppro]
env: spapro
steps:
  - api:
      call: "POST /api/login"
      body: { username: "${username}", password: "wrong-pass-42" }
      expect:
        status: 401
        msg: "contains: 密码"
        data: is_null
