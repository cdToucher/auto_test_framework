# atk M2 实施计划：即时层全链路（diff→plan→run→record→report）+ Agent 工作流

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 跑通即时层：`atk diff/plan` 产出影响面与计划 → Agent CLI 编排执行（复用场景走 `atk run --record-to`，UI 意图由 Agent 用 ego-browser 实测后 `atk record` 回填）→ `atk report` 出统一 HTML 报告。

**Architecture:** atk 只做确定性部分（git 解析、路径归类、场景检索、记录存储、报告渲染）；AI 环节外置给 Agent CLI，工作流以 `.claude/skills/atk-smoke/SKILL.md` 固化。运行记录 `reports/runs/<run_id>/run.yaml` 为单一事实源，证据截图随目录归档。

**Tech Stack:** 既有 M1 栈 + subprocess(git) + fnmatch；UI dogfood 用 ego-browser（本机已装）。

**规格:** `docs/superpowers/specs/2026-08-24-atk-design.md` §4 即时层

---

### Task 1: 模块映射与路径归类

**Files:**
- Create: `atk/diff_analyzer/__init__.py`, `atk/diff_analyzer/modules.py`
- Test: `tests/test_modules.py`

- [ ] **Step 1: 写失败测试**

```python
from atk.diff_analyzer.modules import classify, load_module_map


def test_load_module_map_missing_file(tmp_path):
    assert load_module_map(tmp_path / "nope.yaml") == {}


def test_classify_maps_and_buckets_unmapped():
    module_map = {
        "order": ["src/order/**", "api/**/order/**"],
        "user": ["web/src/views/user/**"],
    }
    files = [
        "src/order/service.go",
        "api/src/main/java/com/x/order/Controller.java",
        "web/src/views/user/List.vue",
        "README.md",
    ]
    groups = classify(files, module_map)
    assert groups["order"] == ["src/order/service.go", "api/src/main/java/com/x/order/Controller.java"]
    assert groups["user"] == ["web/src/views/user/List.vue"]
    assert groups["__unmapped__"] == ["README.md"]


def test_classify_empty_map_all_unmapped():
    groups = classify(["a.py"], {})
    assert groups == {"__unmapped__": ["a.py"]}
```

- [ ] **Step 2: 运行确认失败**

Run: `.venv/bin/pytest tests/test_modules.py -v`
Expected: FAIL (No module named 'atk.diff_analyzer')

- [ ] **Step 3: 实现 modules.py**

```python
"""模块映射：被测代码路径通配 -> 测试模块目录(scenarios/<module>)。"""
import fnmatch
from pathlib import Path

import yaml


def load_module_map(path: Path | str = "config/modules.yaml") -> dict[str, list[str]]:
    p = Path(path)
    if not p.exists():
        return {}
    data = yaml.safe_load(p.read_text(encoding="utf-8")) or {}
    return {str(m): [str(x) for x in pats] for m, pats in data.get("modules", {}).items()}


def classify(files: list[str], module_map: dict[str, list[str]]) -> dict[str, list[str]]:
    """把变更文件按首个命中的模块归组；未命中进 __unmapped__。"""
    result: dict[str, list[str]] = {}
    for f in files:
        norm = f.replace("\\", "/")
        matched = None
        for mod, pats in module_map.items():
            if any(fnmatch.fnmatch(norm, pat) for pat in pats):
                matched = mod
                break
        result.setdefault(matched or "__unmapped__", []).append(norm)
    return result
```

同时创建空文件 `atk/diff_analyzer/__init__.py`。

- [ ] **Step 4: 运行确认通过**

Run: `.venv/bin/pytest tests/test_modules.py -v`
Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
git add atk/diff_analyzer tests/test_modules.py && git commit -m "feat: 模块映射与变更路径归类"
```

---

### Task 2: git 变更文件提取

**Files:**
- Create: `atk/diff_analyzer/git_diff.py`
- Test: `tests/test_git_diff.py`

- [ ] **Step 1: 写失败测试**

```python
import subprocess

import pytest

from atk.diff_analyzer.git_diff import changed_files


def _git(*args, cwd):
    subprocess.run(["git", *args], cwd=cwd, check=True, capture_output=True)


@pytest.fixture
def tiny_repo(tmp_path):
    _git("init", "-q", cwd=tmp_path)
    _git("-c", "user.email=t@t", "-c", "user.name=t", "commit", "--allow-empty", "-m", "init", cwd=tmp_path)
    (tmp_path / "b.txt").write_text("hello")
    _git("add", ".", cwd=tmp_path)
    _git("-c", "user.email=t@t", "-c", "user.name=t", "commit", "-m", "add b", cwd=tmp_path)
    return tmp_path


def test_changed_files_between_refs(tiny_repo):
    assert changed_files("HEAD~1", "HEAD", repo=str(tiny_repo)) == ["b.txt"]


def test_changed_files_bad_ref_raises(tiny_repo):
    with pytest.raises(RuntimeError):
        changed_files("no-such-ref", "HEAD", repo=str(tiny_repo))
```

- [ ] **Step 2: 运行确认失败**

Run: `.venv/bin/pytest tests/test_git_diff.py -v`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 3: 实现 git_diff.py**

```python
"""git 变更文件提取。"""
import subprocess


def changed_files(base: str, head: str = "HEAD", repo: str = ".") -> list[str]:
    """返回 base...head 三点区间的变更文件（相对路径，posix 分隔）。"""
    proc = subprocess.run(
        ["git", "-C", repo, "diff", "--name-only", f"{base}...{head}"],
        capture_output=True,
        text=True,
    )
    if proc.returncode != 0:
        raise RuntimeError(f"git diff 失败: {proc.stderr.strip()}")
    return [line.strip() for line in proc.stdout.splitlines() if line.strip()]
```

- [ ] **Step 4: 运行确认通过**

Run: `.venv/bin/pytest tests/test_git_diff.py -v`
Expected: 2 passed

- [ ] **Step 5: Commit**

```bash
git add atk/diff_analyzer/git_diff.py tests/test_git_diff.py && git commit -m "feat: git 变更文件提取"
```

---

### Task 3: mock 服务增加 Web 页面（供 UI dogfood）

**Files:**
- Modify: `examples/mock_server.py`（do_GET 增加两个 HTML 页面）
- Test: `tests/test_mock_server.py`（追加用例）

- [ ] **Step 1: 写失败测试（追加到 tests/test_mock_server.py）**

```python
def test_web_pages_served(mock_client):
    login = mock_client.get("/")
    assert login.status_code == 200 and "text/html" in login.headers["content-type"]
    assert 'id="username"' in login.text and 'id="password"' in login.text
    app = mock_client.get("/app")
    assert app.status_code == 200 and 'id="orderNo"' in app.text
```

- [ ] **Step 2: 运行确认失败**

Run: `.venv/bin/pytest tests/test_mock_server.py::test_web_pages_served -v`
Expected: FAIL (404)

- [ ] **Step 3: 在 examples/mock_server.py 顶部常量区追加页面、do_GET 增加路由**

```python
_LOGIN_HTML = """<!doctype html><html lang=zh><head><meta charset=utf-8><title>登录</title></head>
<body><h1>演示商城</h1>
<form onsubmit="return doLogin()">
  <input id="username" placeholder="用户名"><br>
  <input id="password" type="password" placeholder="密码"><br>
  <button type="submit">登录</button>
</form><pre id="msg"></pre>
<script>
async function doLogin(){
  const r = await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},
    body:JSON.stringify({username:document.getElementById('username').value,
                         password:document.getElementById('password').value})});
  const j = await r.json();
  if(r.ok){ localStorage.setItem('token', j.data.token); location='/app'; }
  else document.getElementById('msg').textContent='登录失败';
  return false;
}
</script></body></html>"""

_APP_HTML = """<!doctype html><html lang=zh><head><meta charset=utf-8><title>订单</title></head>
<body><h1>订单查询</h1>
<input id="orderNo" placeholder="订单号">
<button onclick="query()">查询</button>
<pre id="out"></pre>
<script>
async function query(){
  const no=document.getElementById('orderNo').value;
  const r=await fetch('/api/orders?orderNo='+encodeURIComponent(no),
    {headers:{Authorization:'Bearer '+localStorage.getItem('token')}});
  const j=await r.json();
  const it=(j.data&&j.data.list&&j.data.list[0])||null;
  document.getElementById('out').textContent= it? ('状态:'+it.status+' 金额:'+it.amount) : '未找到';
}
</script></body></html>"""
```

do_GET 的路由链中插入：

```python
        elif self.path == "/":
            body = _LOGIN_HTML.encode()
            self.send_response(200)
            self.send_header("Content-Type", "text/html; charset=utf-8")
            self.send_header("Content-Length", str(len(body)))
            self.end_headers()
            self.wfile.write(body)
        elif self.path == "/app":
            body = _APP_HTML.encode()
            self.send_response(200)
            self.send_header("Content-Type", "text/html; charset=utf-8")
            self.send_header("Content-Length", str(len(body)))
            self.end_headers()
            self.wfile.write(body)
```

- [ ] **Step 4: 运行确认通过**

Run: `.venv/bin/pytest tests/test_mock_server.py -v`
Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
git add examples/mock_server.py tests/test_mock_server.py && git commit -m "feat: mock 服务增加登录与订单查询页面"
```

---

### Task 4: 运行记录存储层

**Files:**
- Create: `atk/run_store.py`
- Test: `tests/test_run_store.py`

- [ ] **Step 1: 写失败测试**

```python
import pytest

from atk.executors.api_executor import StepResult
from atk.executors.runner import ScenarioResult
from atk.run_store import (
    IntentRecord,
    RunRecord,
    ScenarioSummary,
    StepSummary,
    add_evidence,
    create_run,
    load_run,
    save_run,
    summarize,
)


def test_create_load_save_roundtrip(tmp_path):
    rec = create_run(base_ref="HEAD~1", head_ref="HEAD",
                     affected_files=["a.py"], affected_modules=["demo"],
                     planned_scenarios=["scenarios/demo/login.yaml"], runs_dir=tmp_path)
    loaded = load_run(rec.run_id, runs_dir=tmp_path)
    assert loaded.base_ref == "HEAD~1"
    assert loaded.planned_scenarios == ["scenarios/demo/login.yaml"]
    loaded.intents.append(IntentRecord(title="登录页冒烟", status="pass"))
    save_run(loaded, runs_dir=tmp_path)
    again = load_run(rec.run_id, runs_dir=tmp_path)
    assert again.intents[0].title == "登录页冒烟"


def test_load_run_missing_raises(tmp_path):
    with pytest.raises(KeyError):
        load_run("smoke-nope", runs_dir=tmp_path)


def test_add_evidence_copies_files(tmp_path):
    src = tmp_path / "shot.png"
    src.write_bytes(b"\x89PNG fake")
    rec = create_run(runs_dir=tmp_path / "runs")
    saved = add_evidence(rec, [str(src), str(tmp_path / "ghost.png")], runs_dir=tmp_path / "runs")
    assert saved == ["evidence/shot.png"]
    assert (tmp_path / "runs" / rec.run_id / "evidence" / "shot.png").exists()


def test_summarize_from_scenario_result():
    sc = __import__("atk.store.models", fromlist=["Scenario"]).Scenario(
        scenario="下单", module="order"
    )
    sr = ScenarioResult(
        scenario=sc, passed=False, error_class="assertion",
        steps=[StepResult("POST /x", False, "status 期望 200 实际 500")],
        duration_ms=12, env="it",
    )
    s = summarize(sr)
    assert isinstance(s, ScenarioSummary)
    assert s.name == "下单" and s.passed is False and s.duration_ms == 12
    assert s.steps[0].detail.startswith("status")
```

- [ ] **Step 2: 运行确认失败**

Run: `.venv/bin/pytest tests/test_run_store.py -v`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 3: 实现 run_store.py**

```python
"""即时层运行记录：plan 创建 -> 执行回填 -> 报告渲染的单一事实源。"""
import datetime as dt
import shutil
from pathlib import Path
from typing import Literal

import yaml
from pydantic import BaseModel, Field

RUN_YAML = "run.yaml"
EVIDENCE_DIR = "EvidenceDir".replace("EvidenceDir", "evidence")


class IntentRecord(BaseModel):
    title: str
    status: Literal["pass", "fail", "suspect", "blocked"] = "pass"
    note: str = ""
    evidence: list[str] = Field(default_factory=list)


class StepSummary(BaseModel):
    title: str
    passed: bool
    detail: str = ""
    error_class: str = "assertion"


class ScenarioSummary(BaseModel):
    name: str
    file: str = ""
    module: str = "default"
    priority: str = "P1"
    passed: bool
    error_class: str = "none"
    env: str = ""
    duration_ms: int = 0
    steps: list[StepSummary] = Field(default_factory=list)


class RunRecord(BaseModel):
    run_id: str
    created_at: str
    base_ref: str = ""
    head_ref: str = ""
    affected_files: list[str] = Field(default_factory=list)
    affected_modules: list[str] = Field(default_factory=list)
    planned_scenarios: list[str] = Field(default_factory=list)
    scenarios: list[ScenarioSummary] = Field(default_factory=list)
    intents: list[IntentRecord] = Field(default_factory=list)


def _dump(rec: RunRecord, path: Path) -> None:
    path.write_text(
        yaml.safe_dump(rec.model_dump(), allow_unicode=True, sort_keys=False),
        encoding="utf-8",
    )


def create_run(
    base_ref: str = "",
    head_ref: str = "",
    affected_files: list[str] | None = None,
    affected_modules: list[str] | None = None,
    planned_scenarios: list[str] | None = None,
    runs_dir: Path | str = "reports/runs",
) -> RunRecord:
    now = dt.datetime.now()
    rec = RunRecord(
        run_id=f"smoke-{now.strftime('%Y%m%d-%H%M%S')}",
        created_at=now.isoformat(timespec="seconds"),
        base_ref=base_ref,
        head_ref=head_ref,
        affected_files=affected_files or [],
        affected_modules=affected_modules or [],
        planned_scenarios=planned_scenarios or [],
    )
    d = Path(runs_dir) / rec.run_id
    d.mkdir(parents=True, exist_ok=True)
    _dump(rec, d / RUN_YAML)
    return rec


def _run_yaml(run_id: str, runs_dir: Path | str) -> Path:
    return Path(runs_dir) / run_id / RUN_YAML


def load_run(run_id: str, runs_dir: Path | str = "reports/runs") -> RunRecord:
    f = _run_yaml(run_id, runs_dir)
    if not f.exists():
        raise KeyError(f"运行记录不存在: {run_id}")
    return RunRecord(**yaml.safe_load(f.read_text(encoding="utf-8")))


def save_run(rec: RunRecord, runs_dir: Path | str = "reports/runs") -> Path:
    f = _run_yaml(rec.run_id, runs_dir)
    f.parent.mkdir(parents=True, exist_ok=True)
    _dump(rec, f)
    return f


def add_evidence(
    rec: RunRecord, sources: list[str], runs_dir: Path | str = "reports/runs"
) -> list[str]:
    """拷贝存在的证据文件进运行目录，返回相对引用名；缺失源静默跳过。"""
    ev = Path(runs_dir) / rec.run_id / EVIDENCE_DIR
    ev.mkdir(parents=True, exist_ok=True)
    saved: list[str] = []
    for s in sources:
        src = Path(s)
        if not src.is_file():
            continue
        dest = ev / src.name
        shutil.copy2(src, dest)
        saved.append(f"{EVIDENCE_DIR}/{src.name}")
    return saved


def summarize(result) -> ScenarioSummary:
    """ScenarioResult -> 可序列化摘要（避免循环导入用鸭子类型入参）。"""
    from .executors.runner import ScenarioResult as _SR  # 类型引用仅为文档

    assert isinstance(result, _SR)
    return ScenarioSummary(
        name=result.scenario.scenario,
        file=result.scenario.file,
        module=result.scenario.module,
        priority=result.scenario.priority.value,
        passed=result.passed,
        error_class=result.error_class,
        env=result.env,
        duration_ms=result.duration_ms,
        steps=[
            StepSummary(title=s.title, passed=s.passed, detail=s.detail, error_class=s.error_class)
            for s in result.steps
        ],
    )
```

注意实现时把 `EVIDENCE_DIR` 直接写为 `"evidence"`（上面那行自替换写法仅为本计划排版玩笑，禁止照抄）。

- [ ] **Step 4: 运行确认通过**

Run: `.venv/bin/pytest tests/test_run_store.py -v`
Expected: 4 passed

- [ ] **Step 5: Commit**

```bash
git add atk/run_store.py tests/test_run_store.py && git commit -m "feat: 即时层运行记录存储"
```

---

### Task 5: CLI diff/plan 子命令

**Files:**
- Modify: `atk/cli.py`（新增 import 与两个命令）
- Test: `tests/test_cli_m2.py`

- [ ] **Step 1: 写失败测试**

```python
import re
import subprocess
from pathlib import Path

from typer.testing import CliRunner

from atk.cli import app

SCENARIO = (
    "scenario: 登录冒烟\nmodule: demo\npriority: P0\ntags: [smoke]\nenv: local\n"
    "steps:\n  - api:\n      call: \"GET /ping\"\n      expect: {status: 200}\n"
)


def _git(*args, cwd):
    subprocess.run(["git", *args], cwd=cwd, check=True, capture_output=True)


@pytest.fixture  # 需要 import pytest
def m2_repo(tmp_path):
    _git("init", "-q", cwd=tmp_path)
    cfg = tmp_path / "config"
    cfg.mkdir()
    (cfg / "modules.yaml").write_text('modules:\n  demo: ["src/demo/**"]\n')
    sc = tmp_path / "scenarios" / "demo"
    sc.mkdir(parents=True)
    (sc / "login.yaml").write_text(SCENARIO, encoding="utf-8")
    _git("-C", str(tmp_path), "-c", "user.email=t@t", "-c", "user.name=t", "add", ".")
    _git("-C", str(tmp_path), "-c", "user.email=t@t", "-c", "user.name=t", "commit", "-qm", "init")
    (tmp_path / "src").mkdir()
    (tmp_path / "src" / "demo").mkdir()
    (tmp_path / "src" / "demo" / "logic.py").write_text("x=1")
    _git("-C", str(tmp_path), "add", ".")
    _git("-C", str(tmp_path), "-c", "user.email=t@t", "-c", "user.name=t", "commit", "-qm", "change demo")
    return tmp_path


def test_diff_outputs_groups(m2_repo, monkeypatch):
    monkeypatch.chdir(m2_repo)
    r = CliRunner().invoke(app, ["diff", "--base", "HEAD~1", "--head", "HEAD"])
    assert r.exit_code == 0
    assert '"demo"' in r.output and "src/demo/logic.py" in r.output


def test_plan_creates_run_record(m2_repo, mock_base_url, monkeypatch):
    monkeypatch.chdir(m2_repo)
    (m2_repo / "config" / "environments.yaml").write_text(
        f"local:\n  base_url: {mock_base_url}\n"
    )
    r = CliRunner().invoke(app, ["plan", "--base", "HEAD~1", "--head", "HEAD"])
    assert r.exit_code == 0, r.output
    m = re.search(r"run_id: (\S+)", r.output)
    assert m, r.output
    from atk.run_store import load_run

    rec = load_run(m.group(1), runs_dir=m2_repo / "reports" / "runs")
    assert rec.affected_modules == ["demo"]
    assert rec.planned_scenarios == ["scenarios/demo/login.yaml"]
```

测试文件头部需 `import pytest`。

- [ ] **Step 2: 运行确认失败**

Run: `.venv/bin/pytest tests/test_cli_m2.py -v`
Expected: FAIL（diff/plan 命令不存在）

- [ ] **Step 3: cli.py 新增 import 与命令**

顶部 import 区增加：

```python
import json

from .diff_analyzer.git_diff import changed_files
from .diff_analyzer.modules import classify, load_module_map
from .run_store import create_run, load_run
```

新增两个命令（放在 record 相关命令之前即可）：

```python
@app.command()
def diff(
    base: str = typer.Option("HEAD~1", help="基线引用"),
    head: str = typer.Option("HEAD", help="目标引用"),
    repo: Path = typer.Option(".", help="被测仓库路径"),
    module_map: Path = typer.Option("config/modules.yaml"),
):
    """输出变更文件及模块归属 JSON，供 AI 分析影响面。"""
    files = changed_files(base, head, str(repo))
    groups = classify(files, load_module_map(module_map))
    typer.echo(json.dumps({"base": base, "head": head, "groups": groups},
                          ensure_ascii=False, indent=2))


@app.command()
def plan(
    base: str = typer.Option("HEAD~1"),
    head: str = typer.Option("HEAD"),
    repo: Path = typer.Option("."),
    root: Path = typer.Option("scenarios"),
    module_map: Path = typer.Option("config/modules.yaml"),
    tags: Optional[str] = typer.Option(None, help="复用场景需包含的标签，逗号分隔"),
    runs_dir: Path = typer.Option("reports/runs"),
):
    """创建即时层运行记录并输出执行计划骨架（复用场景清单 + 待补全意图）。"""
    files = changed_files(base, head, str(repo))
    groups = classify(files, load_module_map(module_map))
    affected = sorted(m for m in groups if m != "__unmapped__")
    tag_list = [t.strip() for t in tags.split(",")] if tags else None
    scs, errors = load_scenarios(root)
    reuse = [
        s for s in select(scs)
        if s.module in affected and (not tag_list or set(tag_list) <= set(s.tags))
    ]
    rec = create_run(
        base_ref=base, head_ref=head, affected_files=files,
        affected_modules=affected, planned_scenarios=[s.file for s in reuse],
        runs_dir=str(runs_dir),
    )
    typer.echo(f"run_id: {rec.run_id}")
    typer.echo(f"affected_modules: {', '.join(affected) if affected else '（无映射命中）'}")
    for e in errors:
        typer.secho(f"[跳过] {e}", fg=typer.colors.YELLOW, err=True)
    for u in groups.get("__unmapped__", []):
        typer.echo(f"unmapped: {u}")
    for s in reuse:
        typer.echo(f"reuse: [{s.priority.value}] {s.scenario} ({s.file})")
    typer.echo(
        f"\n下一步（Agent 编排）：\n"
        f"  1. atk run --record-to {rec.run_id}   # 执行复用场景并入记录\n"
        f"  2. 对无覆盖意图用 ego-browser 实测，然后\n"
        f"     atk record {rec.run_id} --title ... --status pass|fail|suspect|blocked [--evidence 截图]\n"
        f"  3. atk report {rec.run_id}"
    )
```

- [ ] **Step 4: 运行确认通过**

Run: `.venv/bin/pytest tests/test_cli_m2.py -v`
Expected: 2 passed

- [ ] **Step 5: Commit**

```bash
git add atk/cli.py tests/test_cli_m2.py && git commit -m "feat: CLI diff/plan 子命令（影响面+运行计划骨架）"
```

---

### Task 6: CLI record 子命令 + run --record-to

**Files:**
- Modify: `atk/cli.py`
- Test: `tests/test_cli_m2.py`（追加）

- [ ] **Step 1: 写失败测试（追加）**

```python
def test_record_appends_intent(m2_repo, tmp_path, monkeypatch):
    monkeypatch.chdir(m2_repo)
    shot = tmp_path / "s.png"
    shot.write_bytes(b"\x89PNG")
    from atk.run_store import create_run

    rec = create_run(runs_dir=m2_repo / "reports" / "runs")
    r = CliRunner().invoke(
        app, ["record", rec.run_id, "--title", "登录页 UI 冒烟",
              "--status", "pass", "--note", "快照断言通过", "--evidence", str(shot)]
    )
    assert r.exit_code == 0, r.output
    from atk.run_store import load_run

    got = load_run(rec.run_id, runs_dir=m2_repo / "reports" / "runs")
    assert got.intents[0].title == "登录页 UI 冒烟"
    assert got.intents[0].evidence == ["evidence/s.png"]
    ev = m2_repo / "reports" / "runs" / rec.run_id / "evidence" / "s.png"
    assert ev.exists()


def test_record_rejects_bad_status(m2_repo, monkeypatch):
    monkeypatch.chdir(m2_repo)
    from atk.run_store import create_run

    rec = create_run(runs_dir=m2_repo / "reports" / "runs")
    r = CliRunner().invoke(app, ["record", rec.run_id, "--title", "x", "--status", "ok"])
    assert r.exit_code != 0


def test_run_record_to_merges_summaries(m2_repo, mock_base_url, monkeypatch):
    monkeypatch.chdir(m2_repo)
    (m2_repo / "config" / "environments.yaml").write_text(
        f"local:\n  base_url: {mock_base_url}\n"
    )
    from atk.run_store import create_run, load_run

    rec = create_run(runs_dir=m2_repo / "reports" / "runs")
    r = CliRunner().invoke(app, ["run", "--record-to", rec.run_id])
    assert r.exit_code == 0, r.output
    got = load_run(rec.run_id, runs_dir=m2_repo / "reports" / "runs")
    assert len(got.scenarios) == 1
    assert got.scenarios[0].name == "登录冒烟"
    assert got.scenarios[0].passed is True
```

说明：`test_run_record_to_merges_summaries` 里 SCENARIO 的步骤是 `GET /ping expect status=200`，对 mock 服务必通过。

- [ ] **Step 2: 运行确认失败**

Run: `.venv/bin/pytest tests/test_cli_m2.py -v`
Expected: 新增 3 个 FAIL

- [ ] **Step 3: 实现——cli.py 增加 record 命令、run 增加参数**

新增 import：

```python
from .run_store import add_evidence, create_run, load_run, save_run, summarize
from .executors.api_executor import StepResult  # noqa: F401 仅为类型可读性
```

record 命令：

```python
_INTENT_STATUS = {"pass", "fail", "suspect", "blocked"}


@app.command()
def record(
    run_id: str,
    title: str = typer.Option(..., help="测试意图标题"),
    status: str = typer.Option("pass", help="pass|fail|suspect|blocked"),
    note: str = typer.Option("", help="结论描述/根因猜测"),
    evidence: Optional[str] = typer.Option(None, help="证据文件路径，逗号分隔"),
    runs_dir: Path = typer.Option("reports/runs"),
):
    """回填一条即时层意图的执行结果（Agent 用 ego-browser 实测后调用）。"""
    if status not in _INTENT_STATUS:
        raise typer.BadParameter(f"status 必须是 {'/'.join(sorted(_INTENT_STATUS))}")
    try:
        rec = load_run(run_id, str(runs_dir))
    except KeyError as e:
        typer.secho(str(e), fg=typer.colors.RED, err=True)
        raise typer.Exit(code=2)
    sources = [p.strip() for p in evidence.split(",")] if evidence else []
    saved = add_evidence(rec, sources, str(runs_dir))
    missing = len([p for p in sources if p]) - len(saved)
    rec.intents.append(IntentRecord(
        title=title, status=status, note=note, evidence=saved,
    ))
    save_run(rec, str(runs_dir))
    warn = f"（{missing} 个证据文件不存在已跳过）" if missing else ""
    typer.echo(f"已记录：{title} [{status}] 证据 {len(saved)} 项{warn}")
```

对应 import 补充 `from .run_store import IntentRecord`（合并进既有 run_store 导入行）。

run 命令签名追加参数与收尾逻辑：

```python
    record_to: Optional[str] = typer.Option(None, help="把本次结果并入指定运行记录"),
    runs_dir: Path = typer.Option("reports/runs"),
```

在 render_html 之后、raise Exit 之前插入：

```python
    if record_to:
        try:
            rec = load_run(record_to, str(runs_dir))
        except KeyError as e:
            typer.secho(str(e), fg=typer.colors.RED, err=True)
            raise typer.Exit(code=2)
        rec.scenarios.extend(summarize(r) for r in report.results)
        save_run(rec, str(runs_dir))
        typer.echo(f"已并入运行记录 {record_to}")
```

- [ ] **Step 4: 运行确认通过**

Run: `.venv/bin/pytest tests/test_cli_m2.py -v`
Expected: 5 passed

- [ ] **Step 5: Commit**

```bash
git add atk/cli.py tests/test_cli_m2.py && git commit -m "feat: record 回填与 run --record-to 并档"
```

---

### Task 7: 运行报告渲染 + report 子命令

**Files:**
- Modify: `atk/reporter/html_reporter.py`（新增 render_run_html）
- Modify: `atk/cli.py`（新增 report 命令）
- Test: `tests/test_run_report.py`

- [ ] **Step 1: 写失败测试**

```python
from atk.cli import app
from atk.reporter.html_reporter import render_run_html
from atk.run_store import IntentRecord, RunRecord, ScenarioSummary, StepSummary, create_run


def _record(tmp_path):
    rec = create_run(base_ref="HEAD~1", head_ref="HEAD",
                     affected_files=["src/a.py"], affected_modules=["demo"],
                     runs_dir=tmp_path / "runs")
    rec.scenarios.append(ScenarioSummary(
        name="登录冒烟", file="scenarios/demo/login.yaml", module="demo",
        priority="P0", passed=True, error_class="none", env="local", duration_ms=33,
        steps=[StepSummary(title="GET /ping", passed=True, detail="200")],
    ))
    rec.intents.append(IntentRecord(
        title="订单页 UI 探索", status="suspect", note="金额显示疑似未刷新",
        evidence=["evidence/s.png"],
    ))
    return rec


def test_render_run_html_contains_all_sections(tmp_path):
    rec = _record(tmp_path)
    out = render_run_html(rec, tmp_path / "runs" / rec.run_id / "report.html")
    html = out.read_text(encoding="utf-8")
    assert "登录冒烟" in html and "HEAD~1" in html
    assert "订单页 UI 探索" in html and "疑似" in html
    assert "evidence/s.png" in html and "src/a.py" in html


def test_report_command(tmp_path, monkeypatch):
    rec = _record(tmp_path)
    monkeypatch.chdir(tmp_path)
    r = app_invoke = None  # 占位防误读
    from typer.testing import CliRunner

    r = CliRunner().invoke(app, ["report", rec.run_id])
    assert r.exit_code == 0, r.output
    assert "报告：" in r.output
```

注意：`r = app_invoke = None` 一行为笔误示例，最终代码必须删除，直接 `r = CliRunner().invoke(app, ["report", rec.run_id])`。

- [ ] **Step 2: 运行确认失败**

Run: `.venv/bin/pytest tests/test_run_report.py -v`
Expected: FAIL (ImportError: render_run_html)

- [ ] **Step 3: html_reporter.py 追加 render_run_html**

文件尾部追加：

```python
_RUN_STATUS = {
    "pass": ("ok", "通过"),
    "fail": ("bad", "失败"),
    "suspect": ("warn", "疑似"),
    "blocked": ("warn", "受阻"),
}


def render_run_html(rec, out_path: Path | str) -> Path:
    out_path = Path(out_path)
    out_path.parent.mkdir(parents=True, exist_ok=True)
    srows, intents = [], []
    for s in rec.scenarios:
        cls, verdict = ("ok", "通过") if s.passed else ("bad", s.error_class)
        srows.append(
            "<tr><td>{}</td><td>{}</td><td>{}</td><td class='{}'>{}</td></tr>".format(
                _html.escape(s.name), _html.escape(s.file), s.priority, cls, verdict
            )
        )
    for i in rec.intents:
        cls, label = _RUN_STATUS.get(i.status, ("bad", i.status))
        imgs = "".join(
            f"<a href='{_html.escape(e)}'><img src='{_html.escape(e)}' "
            "style='max-width:320px;border:1px solid #ddd;margin:4px'></a>"
            for e in i.evidence
        )
        intents.append(
            f"<li class='{cls}'>[{label}] {_html.escape(i.title)}"
            + (f" — {_html.escape(i.note)}" if i.note else "")
            + f"<br>{imgs}</li>"
        )
    doc = (
        "<!doctype html><html lang=zh><head><meta charset=utf-8>"
        f"<title>atk 运行报告 {rec.run_id}</title><style>{_CSS}</style></head><body>"
        f"<h1>即时层运行报告 · {rec.run_id} · {rec.created_at}</h1>"
        f"<div class=sum>基线 {_html.escape(rec.base_ref)} → {_html.escape(rec.head_ref)}　"
        f"受影响模块：{_html.escape(', '.join(rec.affected_modules) or '无')}<br>"
        f"复用场景 {len(rec.scenarios)} 个（通过 {sum(1 for s in rec.scenarios if s.passed)}），"
        f"探索意图 {len(rec.intents)} 条</div>"
        f"<details open><summary>受影响文件（{len(rec.affected_files)}）</summary>"
        f"<pre>{_html.escape(chr(10).join(rec.affected_files))}</pre></details>"
        "<table><tr><th>场景</th><th>文件</th><th>优先级</th><th>结论</th></tr>"
        f"{''.join(srows)}</table>"
        "<h2>探索意图</h2><ul>" + "".join(intents) + "</ul>"
        "</body></html>"
    )
    out_path.write_text(doc, encoding="utf-8")
    return out_path
```

- [ ] **Step 4: cli.py 追加 report 命令**

```python
from .reporter.html_reporter import render_html, render_run_html


@app.command()
def report(
    run_id: str,
    runs_dir: Path = typer.Option("reports/runs"),
):
    """渲染运行记录为 HTML 报告。"""
    try:
        rec = load_run(run_id, str(runs_dir))
    except KeyError as e:
        typer.secho(str(e), fg=typer.colors.RED, err=True)
        raise typer.Exit(code=2)
    out = render_run_html(rec, Path(runs_dir) / run_id / "report.html")
    typer.echo(f"报告：{out}")
```

- [ ] **Step 5: 运行确认通过**

Run: `.venv/bin/pytest tests/test_run_report.py -v`
Expected: 2 passed

- [ ] **Step 6: Commit**

```bash
git add atk/reporter/html_reporter.py atk/cli.py tests/test_run_report.py && git commit -m "feat: 运行记录 HTML 报告与 report 子命令"
```

---

### Task 8: SKILL.md 工作流文档 + README + 配置样例

**Files:**
- Create: `.claude/skills/atk-smoke/SKILL.md`
- Modify: `README.md`（M2 能力段）
- Create: `config/modules.yaml`

- [ ] **Step 1: 写 config/modules.yaml（dogfood 自映射，团队接入时改成被测仓库规则）**

```yaml
# 被测代码路径 -> 测试模块（scenarios/<module>/）。fnmatch 通配。
modules:
  demo:
    - "examples/mock_server.py"
    - "examples/**"
```

- [ ] **Step 2: 写 .claude/skills/atk-smoke/SKILL.md**

```markdown
---
name: atk-smoke
description: 变更驱动的自动化冒烟测试工作流。当开发者要求"跑冒烟/验证这次改动/发布前检查"，或提交涉及业务代码时使用。编排 atk 确定性命令完成影响面分析、回归复用与 UI 探索实测。
---

# atk 即时层冒烟工作流

原则：atk 负责确定性环节；你（AI）负责语义分析与 UI 实测；所有结论必须回填 atk 记录，禁止只口头汇报。

## 流程

1. **影响面**：在被测仓库根目录执行
   `atk diff --base <基线> --head HEAD`
   输出 JSON 的 groups 给出模块归属。对 __unmapped__ 文件，阅读 diff 内容自行判断可能影响的业务模块，并在后续 record 的 note 中注明推断依据。

2. **生成计划**：
   `atk plan --base <基线> --head HEAD`
   记下输出的 run_id 与 reuse 场景清单。若无 reuse 且 unmapped 有业务文件，基于 diff 内容提出 1-5 条最关键的测试意图。

3. **执行复用场景**：
   `atk run --record-to <run_id>`
   失败场景阅读输出中的断言详情定位。

4. **UI 意图实测**（每条意图）：
   - 用 ego-browser 打开目标环境页面，按意图操作；
   - 关键状态截图保存为 PNG 文件（如 /tmp/opencode/atks-<slug>.png）；
   - 回填：`atk record <run_id> --title "<意图>" --status pass|fail|suspect|blocked --note "<证据与根因>" --evidence <截图路径>`
   - 状态语义：pass=断言成立；fail=复现问题（note 必须含重现步骤）；suspect=疑似问题需人定性；blocked=环境/权限原因无法执行。

5. **出报告**：`atk report <run_id>`，向用户报告路径与结论摘要（重点讲 fail/suspect）。

## 禁止事项

- 不得跳过 record 直接口头宣称"测试通过"。
- 不得把 blocked 标成 pass。
- UI 操作遇到登录态缺失先走页面登录流程，仍不行则记 blocked 并说明。
```

- [ ] **Step 3: README.md 的 M2 行更新为已完成并追加用法小节**

在"## M1 已有能力"之后追加：

```markdown
## M2 已有能力（即时层）

`atk diff` 影响面分析 → `atk plan` 生成运行计划 → `atk run --record-to <id>`
执行复用场景 → AI 用 ego-browser 实测新意图后 `atk record <id>` 回填 →
`atk report <id>` 输出统一 HTML 报告（含截图证据）。完整工作流见
`.claude/skills/atk-smoke/SKILL.md`；模块映射配置见 `config/modules.yaml`。
```

并把路线图表 M2 状态改为 ✅。

- [ ] **Step 4: 全量测试回归**

Run: `.venv/bin/pytest -q`
Expected: 全部 passed

- [ ] **Step 5: Commit**

```bash
git add .claude config README.md && git commit -m "docs: atk-smoke 工作流技能与 README M2 更新"
```

---

### Task 9: 全链路 dogfood（含 ego-browser 真实一轮）

**Files:** 无新增；验证性任务。

- [ ] **Step 1: 启动 mock 并制造一次"变更"**

```bash
.venv/bin/python -m examples/mock_server > /tmp/opencode/mock.log 2>&1 &
echo "# touch $(date +%s)" >> examples/mock_server.py
git add examples/mock_server.py && git commit -qm "chore: dogfood 触碰"
```

- [ ] **Step 2: plan → run --record-to**

```bash
PLAN_OUT=$(.venv/bin/atk plan --base HEAD~1 --head HEAD)
echo "$PLAN_OUT"; RUN_ID=$(echo "$PLAN_OUT" | grep -oP 'run_id: \K\S+')
.venv/bin/atk run --record-to "$RUN_ID"
```

Expected: reuse 出现 demo 场景；run 通过且提示已并入记录。

- [ ] **Step 3: ego-browser 实测一条 UI 意图**

用 ego-browser 打开 `http://127.0.0.1:8765/`，以 alice/secret123 登录进入 /app，
在下单接口造一单（或直接用已有 orderNo）查询并截图到 `/tmp/opencode/atks-order.png`，
然后：

```bash
.venv/bin/atk record "$RUN_ID" --title "订单页查询展示" --status pass \
  --note "登录后查询 NO 单号正确展示状态与金额" --evidence /tmp/opencode/atks-order.png
```

Expected: 提示"已记录…证据 1 项"。

- [ ] **Step 4: 渲染报告并核对**

```bash
.venv/bin/atk report "$RUN_ID"
open "$(ls reports/runs/$RUN_ID/report.html)"
grep -c "evidence/atks-order.png" reports/runs/$RUN_ID/report.html
```

Expected: grep 计数 ≥1；浏览器打开可见三段内容（元信息/场景表/探索意图+截图缩略）。

- [ ] **Step 5: 清理与最终回归**

```bash
kill %1 2>/dev/null
git revert --no-edit HEAD   # 撤销 dogfood 触碰提交
.venv/bin/pytest -q
```

Expected: pytest 全绿。

- [ ] **Step 6: Commit（如有残留改动）**

```bash
git status --short; # 应干净
```

---

## 自审记录（Self-Review）

1. **规格覆盖（M2=§4 即时层）**：diff-analyzer(T1,T2)、planner 复用检索(T5)、共享执行器沿用 M1(T6)、reporter 证据链(T7)、ego-browser 实操由 SKILL.md 固化(T8)、全链路验证(T9)。沉淀层固化属 M3 不在本计划。
2. **占位符扫描**：Task 4 Step 3 的 `EVIDENCE_DIR` 自替换写法与 Task 7 测试中占位行均已在文中显式标注"禁止照抄"及最终形态。
3. **类型一致性**：`IntentRecord/ScenarioSummary/RunRecord` 字段在 T4 定义、T5/T6/T7 使用一致；`render_run_html(rec, out)` 签名与 CLI 调用一致；`summarize(result)->ScenarioSummary` 与 run 命令用法一致；`changed_files(base, head, repo)` 参数序与 T2/T5 一致。
